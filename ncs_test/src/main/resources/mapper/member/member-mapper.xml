<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mvc.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis">
    <insert id="insertMember">
        insert into
            member
        values(
            #{userNo}, #{userId}, #{userPw}, #{userName}, #{userAddr}, default
        )
    </insert>
</mapper>